{% extends 'base-super_admin.html.twig' %}


{% block header %}
    {{ 'sa.import_structure_diplome.index.heading'|trans }}
{% endblock %}

{% block headeractions %}
    <div class="header-action">
        <div class="buttons">
            <a href="{{ path('super_admin_homepage') }}" class="btn btn-primary btn-float" data-provide="tooltip"
               data-placement="bottom" title="{{ 'atitle.retour_administration'|trans }}">
                <i class="{{ iconDashboard }}"><span class="sr-only">dashboard</span></i>
            </a>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="card">
        <h4 class="card-title">{{ 'sa.import_structure_diplome.index.heading'|trans }}</h4>
        <div class="card-body">
            <form enctype="multipart/form-data" method="post">
                <div class="form-group">
                    <label for="fichier">Fichier</label>
                    <input type="file" class="form-control" name="fichier" id="fichier">
                </div>
                {# <div class="form-group"> #}
                {# <label for="fichier">Type de fichier</label> #}
                {# <div class="form-check form-check-inline"> #}
                {# <input class="form-check-input" type="radio" name="typeFichier" id="typeFichier1" #}
                {# value="competences"> #}
                {# <label class="form-check-label" for="typeFichier1">Référentiel de compétences</label> #}
                {# </div> #}
                {# <div class="form-check form-check-inline"> #}
                {# <input class="form-check-input" type="radio" name="typeFichier" id="typeFichier2" #}
                {# value="formation"> #}
                {# <label class="form-check-label" for="typeFichier2">Référentiel de formation (PN)</label> #}
                {# </div> #}
                {# </div> #}
                <button type="submit" class="btn btn-success">Importer</button>
            </form>

        </div>
    </div>
{% endblock %}

{% block fabactions %}
    {{ include('composants/fab-buttons.html.twig', {boutons:[
        {
            type:'info',
            href:path('help_article', {sujet: 'super_administration-annee_universitaire'}),
            title: 'aide'|trans,
            icone: 'fa fa-question'
        }
    ]}) }}
{% endblock %}

