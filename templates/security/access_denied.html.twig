{% extends 'base-speciale.html.twig' %}

{% block header %}
    Accès interdit
{%endblock%}
{% block content %}
    <div class="row no-margin h-fullscreen" style="padding-top: 10%">
        <div class="col-12">
            <div class="card card-transparent mx-auto text-center">
                <h1 class="text-secondary lh-1" style="font-size: 200px">403</h1>
                <hr class="w-30px">
                <h3 class="text-uppercase">{{ 'titre.666'|trans }}</h3>

                <p class="lead">{{ exception.message }}</p>
                <p><a href="{{path('administration_index')}}">Accéder à l'administration</a></p>
                <br>
                <ul class="nav nav-primary nav-dotted nav-dot-separated justify-content-center fs-14">
                    <li class="nav-item">
                        <a class="nav-link" href="mailto:{{ settings('MAIL_DEBUG') }}">{{ 'report.problem'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('default_homepage') }}">{{ 'retour.666'|trans }}</a>
                    </li>
                </ul>
            </div>
        </div>


        {{ include('_footer.html.twig') }}
    </div>
{% endblock %}

