{% extends 'base.html.twig' %}

{% block header %}New PlanCoursApc{% endblock %}

{% block content %}
    <div class="card">
        <h4 class="card-title">{{ 'app.personnel.plan_cours.edit.heading'|trans }}</h4>
        <div class="card-body">
            {% form_theme form 'form/my-form.html.twig'  %}
            {{ form(form) }}
        </div>
    </div>
{% endblock %}

