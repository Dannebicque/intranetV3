{% extends "pdf/basepdf.html.twig" %}

{% block content %}
    {% for etudiant in etudiants %}
        {{ include('pdf/unReleveProvisoire.html.twig', {etudiant:etudiant, notes:tabNotes[etudiant.id]}) }}
        {% if not loop.last %}
            <div style="page-break-after: always"></div>
        {% endif %}
    {% endfor %}
{% endblock %}
