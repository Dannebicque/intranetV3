{% extends 'base.html.twig' %}

{% block header %}
    {{ 'justificatif.absence.semestre.heading'|trans({'%semestre%':semestre.libelle})|raw }}
{% endblock %}

{% block headerright %}
    <div class="right gap-items-2">
        {% for sem in user_data.semestresActifs %}
            <a href="{{ path('administration_absences_justificatif_semestre_liste', {semestre:sem.id}) }}"
               class="btn btn-round {% if sem.id == semestre.id %}btn-primary{% endif %}"
               title="{{ 'atitle.changer.semestre'|trans }}"
               data-bs-toggle="tooltip"
               data-bs-placement="bottom"
            >{{ sem.libelle }}</a>
        {% endfor %}
    </div>
{% endblock %}

{% block headeractions %}
    <div class="header-action">
        <div class="buttons">
            <a href="{{ path('administration_absence_justificatif_new', {semestre: semestre.id}) }}"
               class="btn btn-info btn-sm btn-float"
               title="{{ 'atitle.creer_nouveau'|trans }}"
               data-bs-toggle="tooltip" data-bs-placement="bottom"
            >
                <i class="fas fa-plus"></i>
                <span class="sr-only">{{ 'atitle.creer_nouveau'|trans }}</span>
            </a>
            {# <a href="{{ path('administration_absence_justificatif_delete_all', {semestre: semestre.id}) }}" #}
            {# class="btn btn-danger btn-sm btn-float supprimer" data-bs-toggle="tooltip" #}
            {# data-bs-placement="bottom" title="Supprimer tous les justificatifs d'absence pour l'année {{ semestre #}
            {# .annee.libelle }}"><i class="{{ iconDelete }}"></i></a> #}
            <a href="{{ path('administration_semestre_index', {semestre:semestre.id}) }}"
               class="btn btn-primary btn-float" data-bs-toggle="tooltip"
               data-bs-placement="bottom" title="{{ 'atitle.retour_semestres'|trans }}"><i class="fas fa-arrow-left"></i></a>
            <a href="{{ path('administration_index') }}" class="btn btn-primary btn-float" data-bs-toggle="tooltip"
               data-bs-placement="bottom" title="{{ 'atitle.retour_administration'|trans }}"><i class="{{ iconAdmin }}"><span class="sr-only">Administration</span></i></a>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-body">
            {{ table_render(table) }}
{#            <table class="table table-striped table-bordered">#}
{#                <thead>#}
{#                <tr>#}
{#                    <th>{{ 'table.etudiant'|trans }}</th>#}
{#                    <th>{{ 'table.groupes'|trans }}</th>#}
{#                    <th>{{ 'table.periode_absence'|trans }}</th>#}
{#                    <th>{{ 'table.created'|trans }}</th>#}
{#                    <th>{{ 'table.motif'|trans }}</th>#}
{#                    <th>{{ 'table.etat_justificatif'|trans }}</th>#}
{#                    <th>{{ 'table.actions'|trans }}</th>#}
{#                </tr>#}
{#                </thead>#}
{#                <tbody>#}
{#                {% for justificatif in justificatifs %}#}
{#                    <tr>#}
{#                        <td>{{ justificatif.etudiant ? justificatif.etudiant.display : 'non.defini'|trans }}</td>#}
{#                        <td>#}
{#                            {{ justificatif.etudiant ? justificatif.etudiant|displayGroupes : 'non.defini'|trans }}#}
{#                        </td>#}
{#                        <td>{{ justificatif.dateHeureDebut ? justificatif.dateHeureDebut|format_datetime('long',#}
{#                                'short') :#}
{#                            'non.defini'|trans }}#}
{#                            au {{ justificatif.dateHeureFin ? justificatif.dateHeureFin|format_datetime('long', 'short') :#}
{#                            'non.defini'|trans }}</td>#}
{#                        <td>{{ justificatif.created ? justificatif.created|format_date('long') : 'non.defini'|trans }} </td>#}
{#                        <td>{{ justificatif.motif }}</td>#}
{#                        <td>{{ justificatif.etatLong }}</td>#}
{#                        <td>#}
{#                            <button#}
{#                                    class="btn btn-info btn-outline btn-square"#}
{#                                    data-provide="modaler tooltip"#}
{#                                    data-url="{{ path('administration_absence_justificatif_details', {uuid: justificatif.uuidString}) }}"#}
{#                                    data-title="{{ 'detail.absencejustificatif'|trans }}"#}
            {# title="{{ 'atitle.details'|trans }}"><i #}
{#                                        class="fas fa-eye"></i></button>#}
{#                            {% if justificatif.etat == 'A' %}#}
{#                                <button#}
{#                                        class="btn btn-success btn-outline bx_{{ justificatif.uuidString }}"#}
{#                                ><i#}
{#                                            class="fas fa-check"></i> Accepté#}
{#                                </button>#}
{#                                <button#}
{#                                        data-justificatif="{{ justificatif.uuidString }}"#}
            {# class="btn btn-danger btn-outline btn-square justificatif-annuler bx_{{ justificatif.uuidString }}" data-bs-toggle="tooltip" data-bs-placement="bottom" #}
{#                                        title="{{ 'atitle.annuler'|trans }}">#}
{#                                    <i class="fas fa-undo"></i>#}
{#                                </button>#}
{#                            {% elseif justificatif.etat == 'R' %}#}
{#                                <button#}
{#                                        class="btn btn-warning btn-outline bx_{{ justificatif.uuidString }}"><i#}
{#                                            class="fas fa-ban"></i> Refusé#}
{#                                </button>#}
{#                                <button#}
{#                                        data-justificatif="{{ justificatif.uuidString }}"#}
            {# class="btn btn-danger btn-outline btn-square justificatif-annuler bx_{{ justificatif.uuidString }}" data-bs-toggle="tooltip" data-bs-placement="bottom" #}
{#                                        title="{{ 'atitle.annuler'|trans }}">#}
{#                                    <i class="fas fa-undo"></i>#}
{#                                </button>#}
{#                            {% else %}#}
{#                                <a href="#"#}
            {# class="btn btn-success btn-outline btn-square justificatif-accepte bx_{{ justificatif.uuidString }}" data-bs-toggle="tooltip" data-bs-placement="bottom" #}
{#                                   data-justificatif="{{ justificatif.uuidString }}"#}
            {# title="{{ 'atitle.accepter.le.justificatif'|trans }}"> #}
{#                                    <i class="fas fa-check"></i></a>#}
{#                                <a href="#"#}
            {# class="btn btn-warning btn-outline btn-square justificatif-refuse bx_{{ justificatif.uuidString }}" data-bs-toggle="tooltip" data-bs-placement="bottom" #}
{#                                   data-justificatif="{{ justificatif.uuidString }}"#}
            {# title="{{ 'atitle.refuser.le.justificatif'|trans }}"><i #}
{#                                            class="fas fa-ban"></i></a>#}

{#                                <a href="{{ path('administration_absence_justificatif_delete', {id: justificatif.id}) }}"#}
{#                                   data-csrf="{{ csrf_token('delete' ~ justificatif.id) }}"#}
            {# data-bs-toggle="tooltip" data-bs-placement="bottom" #}
{#                                   title="{{ 'atitle.supprimer'|trans }}"#}
{#                                   class="btn btn-danger btn-outline btn-square supprimer bx_{{ justificatif.uuidString }}"><i#}
{#                                            class="{{ iconDelete }}"></i></a>#}
{#                            {% endif %}#}

{#                            </td>#}
{#                    </tr>#}
{#                {% endfor %}#}
{#                </tbody>#}
{#            </table>#}
        </div>
    </div>
{% endblock %}

{% block fabactions %}
    {{ include('composants/fab-button-help.html.twig', {
        href:path('help_article', {sujet: 'administration-justificatif-absence'})
    }
    ) }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('adm.justificatifs') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('adm.justificatifs') }}
{% endblock %}
