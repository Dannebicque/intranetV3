<div class="col-md-6 col-lg-6 col-sm-12">
    <div class="card administration">
        <header class="card-header" style="justify-content: space-between">
            <div class="card-header-actions" style="">
                <i class="fa-solid fa-file-lines fa-2x"></i>
                <h2 class="card-title">{{ 'adm.documents'|trans }}</h2>
            </div>

            <div class="card-header-actions" style="">
                <a href="{{ path('administration_document_new') }}" class="btn btn-sm btn-primary text-white"
                   title="Ajouter un document" data-bs-toggle="tooltip" data-bs-placement="bottom">
                    <i class="fas fa-plus"></i><span
                            class="sr-only">ajouter un document</span></a>
            </div>
        </header>

        <div class="card-body">
            <div class="row">
                <div class="col-12 d-grid d-block mb-2">
                    <a href="{{ path('administration_document_index', {source: constant
                        ('App\\Entity\\Document::DOCUMENTS')}) }}">Gestion des documents</a>
                    <a href="{{ path('administration_type_document_index', {source:constant
                        ('App\\Entity\\Document::DOCUMENTS')}) }}">Gestion des catégories de documents</a>
                    <a href="{{ path('administration_document_new') }}">Ajouter un document</a>
                </div>
            </div>
        </div>
    </div>
</div>
