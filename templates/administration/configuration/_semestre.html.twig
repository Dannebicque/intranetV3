{{ semestre.libelle }}
<div class="row">
    <div class="col-md-6">
        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.semestre_actif'|trans }}</strong></p>
                <p>{{ 'help.semestre_actif'|trans }}</p>
            </div>
            <label class="switch" for="actif_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.actif ? 'checked': '' }}
                       class="changeOption"
                       name="actif_{{ semestre.id }}"
                       id="actif_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.ppn_semestre'|trans }}</strong></p>
                <p>{{ 'help.ppn_semestre'|trans }}</p>
            </div>
            <label class="sr-only" for="ppnSemestre_{{ semestre.id
            }}">ppnSemestre_{{ semestre.id }}</label>
            <select class="form-select col-sm-6 changePpnSemestre"
                    name="ppnSemestre_{{ semestre.id }}"
                    id="ppnSemestre_{{ semestre.id }}"
                    data-type="semestre"
                    data-id="{{ semestre.id }}">
                <option value="">{{ 'non.selectione'|trans }}
                    / {{ 'aucun'|trans }}</option>
                {% for ppn in semestre.diplome.ppns %}
                    <option value="ppn{{ ppn.id }}"
                            {% if semestre.ppnActif != null and semestre.ppnActif
                            .id == ppn.id %}selected{% endif %}>

                        {{ ppn.libelle }} ({{ ppn.annee }})
                    </option>
                {% endfor %}
            </select>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_mail_releve'|trans }}</strong></p>
                <p>{{ 'help.opt_mail_releve'|trans }}</p>
            </div>
            <label class="switch" for="optMailReleve_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optMailReleve ? 'checked': '' }}
                       class="changeOption"
                       name="optMailReleve_{{ semestre.id }}"
                       id="optMailReleve_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_destinataire_mail_releve'|trans }}</strong></p>
                <p>{{ 'help.opt_destinataire_mail_releve'|trans }}</p>
            </div>
            <label class="sr-only" for="optDestMailReleve_{{ semestre.id }}">optDestMailReleve_{{ semestre.id }}</label>
            <select class="form-select col-sm-6 changeOptionSelect"
                    name="optDestMailReleve_{{ semestre.id }}"
                    id="optDestMailReleve_{{ semestre.id }}"
                    data-type="semestre"
                    data-id="{{ semestre.id }}">
                <option value="">{{ 'non.selectione'|trans }}
                    / {{ 'aucun'|trans }}</option>
                {% for personnel in user_data.personnels %}
                    <option value="pers{{ personnel.id }}"
                            {% if semestre.optDestMailReleve != null and semestre.optDestMailReleve.id == personnel.id %}selected{% endif %}>{{ personnel.display }}</option>
                {% endfor %}
            </select>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_mail_modification_note'|trans }}</strong></p>
                <p>{{ 'help.opt_mail_modification_note'|trans }}</p>
            </div>
            <label class="switch" for="optMailModificationNote_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optMailModificationNote ? 'checked': '' }}
                       class="changeOption"
                       name="optMailModificationNote_{{ semestre.id }}"
                       id="optMailModificationNote_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p>
                    <strong>{{ 'table.opt_destinataure_mail_modification_note'|trans }}</strong>
                </p>
                <p>{{ 'help.opt_destinataure_mail_modification_note'|trans }}</p>
            </div>
            <label class="sr-only" for="optDestMailModifNote_{{ semestre.id }}">optDestMailModifNote_{{ semestre.id }}</label>
            <select class="form-control col-sm-6 changeOptionSelect"
                    name="optDestMailModifNote_{{ semestre.id }}"
                    id="optDestMailModifNote_{{ semestre.id }}"
                    data-type="semestre"
                    data-id="{{ semestre.id }}">
                <option value="">{{ 'non.selectione'|trans }}
                    / {{ 'aucun'|trans }}</option>
                {% for personnel in user_data.personnels %}
                    <option value="pers{{ personnel.id }}"
                            {% if semestre.optDestMailModifNote != null and semestre.optDestMailModifNote.id == personnel.id %}selected{% endif %}>{{ personnel.display }}</option>
                {% endfor %}
            </select>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_mail_absence_responsable'|trans }}</strong></p>
                <p>{{ 'help.opt_mail_absence_responsable'|trans }}</p>
            </div>
            <label class="switch" for="optMailAbsenceResp_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optMailAbsenceResp ? 'checked': '' }}
                       class="changeOption"
                       name="optMailAbsenceResp_{{ semestre.id }}"
                       id="optMailAbsenceResp_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p>
                    <strong>{{ 'table.opt_destinataure_mail_absence_responsable'|trans }}</strong>
                </p>
                <p>{{ 'help.opt_destinataure_mail_absence_responsable'|trans }}</p>
            </div>
            <label class="sr-only" for="optDestMailAbsenceResp_{{ semestre.id }}">optDestMailAbsenceResp_{{ semestre.id }}</label>
            <select class="form-select col-sm-6 changeOptionSelect"
                    name="optDestMailAbsenceResp_{{ semestre.id }}"
                    id="optDestMailAbsenceResp_{{ semestre.id }}"
                    data-type="semestre"
                    data-id="{{ semestre.id }}">
                <option value="">{{ 'non.selectione'|trans }}
                    / {{ 'aucun'|trans }}</option>
                {% for personnel in user_data.personnels %}
                    <option value="pers{{ personnel.id }}"
                            {% if semestre.optDestMailAbsenceResp != null and semestre.optDestMailAbsenceResp.id == personnel.id %}selected{% endif %}>{{ personnel.display }}</option>
                {% endfor %}
            </select>
        </div>
        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_mail_assistante_justificatif_absence'|trans }}</strong></p>
                <p>{{ 'help.opt_mail_assistante_justificatif_absence'|trans }}</p>
            </div>
            <label class="switch" for="optMailAssistanteJustificatifAbsence_{{
            semestre.id }}">
                <input type="checkbox" {{ semestre.optMailAssistanteJustificatifAbsence ? 'checked': '' }}
                       class="changeOption"
                       name="optMailAssistanteJustificatifAbsence_{{ semestre.id }}"
                       id="optMailAssistanteJustificatifAbsence_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>
    </div>
    <div class="col-md-6">
        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_evaluation_pre_initialisee'|trans }}</strong>
                </p>
                <p>{{ 'help.opt_evaluation_pre_initialisee'|trans }}</p>
            </div>
            <label class="switch" for="optEvaluationPreInitialisee_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optEvaluationPreInitialisee ? 'checked': '' }}
                       class="changeOption"
                       name="optEvaluationPreInitialisee_{{ semestre.id }}"
                       id="optEvaluationPreInitialisee_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>
        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_evaluation_visible'|trans }}</strong></p>
                <p>{{ 'help.opt_evaluation_visible'|trans }}</p>
            </div>
            <label class="switch" for="optEvaluationVisible_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optEvaluationVisible ? 'checked': '' }}
                       class="changeOption"
                       name="optEvaluationVisible_{{ semestre.id }}"
                       id="optEvaluationVisible_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_evaluation_modifiable'|trans }}</strong></p>
                <p>{{ 'help.opt_evaluation_modifiable'|trans }}</p>
            </div>
            <label class="switch" for="optEvaluationModifiable_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optEvaluationModifiable ? 'checked': '' }}
                       class="changeOption"
                       name="optEvaluationModifiable_{{ semestre.id }}"
                       id="optEvaluationModifiable_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_mail_absence_etudiant'|trans }}</strong></p>
                <p>{{ 'help.opt_mail_absence_etudiant'|trans }}</p>
            </div>
            <label class="switch" for="optMailAbsenceEtudiant_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optMailAbsenceEtudiant ? 'checked': '' }}
                       class="changeOption"
                       name="optMailAbsenceEtudiant_{{ semestre.id }}"
                       id="optMailAbsenceEtudiant_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_penalite_absence'|trans }}</strong></p>
                <p>{{ 'help.opt_penalite_absence'|trans }}</p>
            </div>
            <label class="switch" for="optPenaliteAbsence_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optPenaliteAbsence ? 'checked': '' }}
                       class="changeOption"
                       name="optPenaliteAbsence_{{ semestre.id }}"
                       id="optPenaliteAbsence_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_point_penalite_absence'|trans }}</strong></p>
                <p>{{ 'help.opt_point_penalite_absence'|trans }}</p>
            </div>
            <label class="sr-only" for="optPointPenaliteAbsence_{{ semestre.id }}">optPointPenaliteAbsence_{{ semestre.id }}</label>
            <select class="form-select col-sm-6 changeOptionSelect"
                    name="optPointPenaliteAbsence_{{ semestre.id }}"
                    id="optPointPenaliteAbsence_{{ semestre.id }}"
                    data-type="semestre"
                    data-id="{{ semestre.id }}">
                {% for i in 1 .. 15 %}
                    <option value="{{ i/10 }}"
                            {% if semestre.optPointPenaliteAbsence == i/10 %}selected{% endif %}>{{ i/10 }} {{ 'point.s'|trans }}</option>
                {% endfor %}
            </select>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>{{ 'table.opt_nb_jours_saisie_absence'|trans }}</strong></p>
                <p>{{ 'help.opt_nb_jours_saisie_absence'|trans }}</p>
            </div>
            <label class="sr-only" for="optNbJoursSaisieAbsence_{{ semestre.id }}">optNbJoursSaisieAbsence_{{ semestre.id }}</label>
            <select class="form-select col-sm-6 changeOptionSelect"
                    name="optNbJoursSaisieAbsence_{{ semestre.id }}"
                    id="optNbJoursSaisieAbsence_{{ semestre.id }}"
                    data-type="semestre"
                    data-id="{{ semestre.id }}">
                {% for i in 0 .. 30 %}
                    <option value="{{ i }}"
                            {% if semestre.optNbJoursSaisieAbsence == i %}selected{% endif %}>{{ i }} {{ 'jour.s'|trans }}</option>
                {% endfor %}
            </select>
        </div>

        <div class="media">
            <div class="media-body">
                <p><strong>Afficher le bilan de semestre (uniquement en B.U .T.)</strong></p>
                <p>Affiche un onglet, dans le profil, visible des étudiants avec le
                   détail des notes et des moyennes pour le B.U.T. Par défaut les
                   ensiegnants y ont toujours accès.</p>
            </div>
            <label class="switch" for="optBilanSemestre_{{ semestre.id }}">
                <input type="checkbox" {{ semestre.optBilanSemestre ? 'checked': '' }}
                       class="changeOption"
                       name="optBilanSemestre_{{ semestre.id }}"
                       id="optBilanSemestre_{{ semestre.id }}"
                       data-type="semestre"
                       data-id="{{ semestre.id }}">
                <span class="switch-indicator"></span>
            </label>
        </div>

    </div>
</div>
