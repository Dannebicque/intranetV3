{{ include('mails/stage_'~mail~'.txt.twig') }}
{# todo: comment gérer les variables du twig ? avoir un template par défaut avec le même formattage des champs ? Du
coup traitement uniquement si provenant de la BDD ou du defaut. #}

