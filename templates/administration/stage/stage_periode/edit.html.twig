{% extends 'base.html.twig' %}

{% block header %}
    {{ 'adm.stage_periode.modification.heading'|trans }}
{% endblock %}

{% block headeractions %}
    <div class="header-action">
        {{ include('composants/adm-buttons-edit.html.twig', {elt:'stage_periode', id: stage_periode.uuidString}) }}
    </div>
{% endblock %}

{% block content %}
    <div class="card">
        <h4 class="card-title">{{ 'adm.stage_periode.modification.heading'|trans }}</h4>
        <div class="card-body">
            <twig:Alert type="warning">
                La suppression d'une période de stage entrainera la suppression de toutes les données associées
                (stage d'étudiants, offres de stages, ...). La suppression ne devrait être utilisée qu'en cas
                d'erreur dans la création de la période. Il est préférable de conserver
                cette période de stage et de créer une autre période de stage pour une nouvelle année universitaire.
            </twig:Alert>
            <br>
            {% form_theme form 'form/my-form.html.twig' %}
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">{{ button_label|default('label.update'|trans) }}</button>
            {{ form_end(form) }}
            <br>
            <twig:Alert type="info">
                Tous les champs avec un <span class="text-red">*</span> sont obligatoires
            </twig:Alert>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('adm.stage') }}
{% endblock %}

