{% extends 'base.html.twig' %}

{% block header %}
    {{ 'adm.stage_periode_soutenance.index.heading'|trans }}
{% endblock %}

{% block headeractions %}
    <div class="header-action">
        <div class="buttons">
            {#<a href="{{ path('administration_stage_soutenance_new') }}" class="btn btn-info btn-sm btn-float"#}
            {#title="{{ 'atitle.creer_nouveau'|trans }}" data-provide="tooltip" data-placement="bottom"><i#}
            {#class="fas fa-plus"></i></a>#}
            <a href="{{ path('administration_stage_periode_gestion', {uuid: stagePeriode.uuidString}) }}"
               class="btn btn-primary btn-float" data-provide="tooltip"
               data-placement="bottom" title="{{ 'atitle.retour.stage_periode_gestion'|trans }}"><i
                        class="material-icons">arrow_back</i></a>

        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="card">
        <h4 class="card-title">{{ 'adm.stage_periode_soutenance.index.heading'|trans }}</h4>
        <div class="card-body">
            {#todo: a faire#}
        </div>
    </div>
{% endblock %}
