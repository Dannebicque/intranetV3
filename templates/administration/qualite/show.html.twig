{% extends 'base.html.twig' %}

{% block header %}
    {{ 'adm.qualite.show.heading'|trans }} | {{ semestre.display }}
{% endblock %}

{% block headeractions %}
        <div class="header-action">
            <div class="buttons">
                    <a href="{{ path('administration_qualite_index') }}"
                       class="btn btn-primary btn-float"
                       title="{{ 'atitle.retour_liste'|trans }}"
                       data-bs-toggle="tooltip" data-bs-placement="bottom"
                    >
                        <i class="fas fa-arrow-left"></i>
                        <span class="sr-only">{{ 'atitle.retour_liste'|trans }}</span>
                    </a>
            </div>
        </div>
{% endblock %}

{% block content %}
    <div class="card">
        <h4 class="card-title">{{ 'adm.qualite.show.heading'|trans }} | {{ semestre.display }}</h4>
        <div class="card-body">
            {{ include('questionnaire/commun/_tableauReponses.html.twig') }}
        </div>
    </div>
{% endblock %}

{% block fabactions %}
    {{ include('composants/fab-button-help.html.twig', {
        href:path('help_article', {sujet: 'administration_qualite'}),
    }) }}
{% endblock %}


