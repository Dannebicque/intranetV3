{% extends 'base.html.twig' %}

{% block title %}QuizzQuestion{% endblock %}

{% block body %}
    <h1>QuizzQuestion</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ quizz_question.id }}</td>
        </tr>
        <tr>
            <th>Created</th>
            <td>{{ quizz_question.created ? quizz_question.created|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ quizz_question.updated ? quizz_question.updated|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>Libelle</th>
            <td>{{ quizz_question.libelle }}</td>
        </tr>
        <tr>
            <th>Help</th>
            <td>{{ quizz_question.help }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ quizz_question.type }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('quizz_question_index') }}">back to list</a>

    <a href="{{ path('quizz_question_edit', {'id': quizz_question.id}) }}">edit</a>

    {{ include('quizz_question/_delete_form.html.twig') }}
{% endblock %}
