{% extends 'base-super_admin.html.twig' %}


{% block header %}
    {{ 'sadm.configuration.index.heading'|trans }}
{% endblock %}

{% block headeractions %}
    <div class="header-action">
        {{ include('composants/sadm-buttons-index.html.twig', {elt:'configuration'}) }}
    </div>
{% endblock %}

{% block content %}
    {# feature: création directement dans le datatable ??? #}
    <div class="card">
        <h4 class="card-title">{{ 'sadm.configuration.index.heading'|trans }}</h4>
        <div class="card-body">
            <table class="table table-striped table-bordered" data-provide="datatables">
                <thead>
                <tr>
                    <th>{{ 'table.cle'|trans }}</th>
                    <th>{{ 'table.valeur'|trans }}</th>
                    <th>{{ 'table.actions'|trans }}</th>
                </tr>
                </thead>
                <tbody>
                {% for configuration in configurations %}
                    <tr>
                        <td>{{ configuration.cle }}</td>
                        <td>{{ configuration.valeur }}</td>
                        <td>
                            {{ include('composants/sadm-buttons-table-index.html.twig', {elt:'configuration', id: configuration.id}) }}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">{{ 'no.record.found'|trans }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

{% block fabactions %}
    {{ include('composants/fab-buttons.html.twig', {boutons:[
        {type: 'info',
            href: path('sa_configuration_export', {_format:'pdf'}),
            title: 'exporter.pdf'|trans,
            icone : "fa fa-file-pdf-o"},
        {
            type:'info',
            href:path('sa_configuration_export', {_format:'csv'}),
            title: 'exporter.csv'|trans,
            icone: 'fa fa-file-text-o'
        },
        {
            type:'info',
            href:path('sa_configuration_export', {_format:'xlsx'}),
            title: 'exporter.xls'|trans,
            icone: 'fa fa-file-excel-o'
        },
        {
            type:'info',
            href:path('sa_configuration_new'),
            title: 'ajouter'|trans,
            icone: 'ti-plus'
        },
        {
            type:'info',
            href:path('help_article', {sujet: 'super-administration_configuration'}),
            title: 'aide'|trans,
            icone: 'fa fa-question'
        }
    ]}) }}
{% endblock %}
