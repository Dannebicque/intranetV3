{% extends 'base.html.twig' %}

{% block title %}Bac{% endblock %}

{% block body %}
    <h1>Bac</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bac.id }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ bac.created ? bac.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ bac.updated ? bac.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ bac.libelle }}</td>
            </tr>
            <tr>
                <th>LibelleLong</th>
                <td>{{ bac.libelleLong }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bac_index') }}">back to list</a>

    <a href="{{ path('bac_edit', {'id': bac.id}) }}">edit</a>

    {{ include('bac/_delete_form.html.twig') }}
{% endblock %}
