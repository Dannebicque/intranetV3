Bonjour,<br><br>

<p>La demande d'autorisation de déplacement professionnel de {{ covidAttestationPersonnel.personnel.display }}
   initialement déposée le {{ covidAttestationPersonnel
    .created|format_date('short') }} a été modifiée et doit être validée.</p>

<p>Cette demande concerne la période :</p>

<ul>
    {% for creneau in covidAttestationPersonnel.covidCreneauPresences %}
        <li> {{ creneau.display }}</li>>
    {% endfor %}
</ul>
<p>Pour le motif: {{ covidAttestationPersonnel.motif|trans }}</p>

<p>Vous pouvez voir la demande ici : <a href="{{ url('administration_covid_attestation_personnel_show',
        {id:covidAttestationPersonnel.id}) }}">Voir la demande de {{ covidAttestationPersonnel.personnel.display }}</a>
</p>

<p>Cordialement,</p>
<br><br>
<i>Ce message est un message automatique envoyé depuis l'intranet de l'{{ settings('NOM_IUT') }}.</i>
