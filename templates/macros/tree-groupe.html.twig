{% macro tree(items, space, semestre, groupes,typeGroupes,parcours) %}
    {% import _self as m %}
    {% if items %}

            {% for groupe in items %}
                {{ include('administration/groupe/_ligne_groupe.html.twig') }}
                {{ m.tree(groupe.enfants, space+1, semestre, groupes,typeGroupes,parcours) }}
            {% endfor %}
    {% endif %}
{% endmacro %}
