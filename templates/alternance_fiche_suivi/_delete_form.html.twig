<form method="post" action="{{ path('alternance_fiche_suivi_delete', {'id': alternance_fiche_suivi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alternance_fiche_suivi.id) }}">
    <button class="btn">Delete</button>
</form>
