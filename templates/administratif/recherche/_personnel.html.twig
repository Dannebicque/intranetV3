<form class="form" action="{{ path('administratif_scolarite_recherche', {type:'personnel'}) }}"
      method="POST">
    <div class="form-group">
        <label class="label">Recherche :</label>
        <input type="text" class="form-control" placeholder="Nom, Prénom, numéro harpège, login, ..." name="search">
    </div>
    <div class="form-group">
        <input type="submit" class="btn btn-primary btn-block" value="Rechercher">
    </div>
</form>
