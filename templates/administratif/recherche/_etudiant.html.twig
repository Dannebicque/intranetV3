<form class="form" action="{{ path('administratif_scolarite_recherche', {type:'etudiant'}) }}"
      method="POST">
    <div class="form-group">
        <label class="label">Recherche :</label>
        <input type="text" class="form-control" placeholder="Nom, Prénom, numéro étudiant login, ..." name="search">
    </div>
    <div class="form-group">
        <input type="submit" class="btn btn-primary btn-block" value="Rechercher">
    </div>
</form>
